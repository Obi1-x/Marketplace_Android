<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.obi1.a3ade">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".CleanupService"
            android:enabled="true"
            android:exported="true"></service>

        <activity
            android:name=".StockUpProductActivity"
            android:label="Stock Up"
            android:parentActivityName=".ProductViewActivity"></activity>
        <activity
            android:name=".ProfileActivity"
            android:label="Profile"
            android:parentActivityName=".Dashboard" />
        <activity
            android:name=".CartActivity"
            android:label="@string/title_activity_product_vew1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProductViewActivity"
            android:label="Product View" />
        <activity
            android:name=".StoreActivity"
            android:label="Store workshop" />
        <activity
            android:name=".Dashboard"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>